package me.hapyl.fight.game.maps;

import javax.annotation.Nonnull;

public class CommissionLevel extends Level {
    protected CommissionLevel(@Nonnull EnumLevel handle, @Nonnull String name) {
        super(handle, name);
    }
}
